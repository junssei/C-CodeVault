#include <iostream>

using namespace std;

int main(){
	
	int a, b, c, d;
	
	cout << "Enter four numbers: " << endl;
	cin >> a, b, c, d;
	
	
	//lowest to highest
	if(a < b && a < c && a < d) {
			cout << " " << a;
		if (b < c && b < d)
			cout << " " << b;
			if 
	}
	
	
	
}